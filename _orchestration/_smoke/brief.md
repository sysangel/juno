Write a single TypeScript function `add(a: number, b: number): number` that returns the sum.

Respond with a SINGLE markdown document. Put a line `=== FILE: src/add.ts ===` immediately followed by a fenced code block containing the full file contents. Then a `=== NOTES ===` section under 30 words. Do not write to the filesystem; output only this document.
