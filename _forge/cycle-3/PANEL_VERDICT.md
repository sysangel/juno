{
  "n": 3,
  "item": {
    "title": "Streaming health checks",
    "gap": "readLines() in claudeCliClient.ts iterates stdout with a bare for-await and no timeout. A hung claude -p process (network stall, OS-level hang, claude CLI deadlock) causes the UI to freeze indefinitely with no recovery. Hermes uses a 90s stale-stream detector + 60s read timeout to catch this early. The ledger shows two failed cycle attempts — cycle 1 was escalated with an unsubstantiated frozen-seam concern (no actual seam touch is required; see sketch), and cycle 2 was rejected solely because the working tree was on main rather than a forge/* branch (a process violation, not a feature defect). The feature itself is constitutionally clean."
  },
  "outcome": "merged",
  "branch": "forge/streaming-health-checks",
  "writerPath": "degraded",
  "verdicts": [
    {
      "judge": "Correctness Assay (Stage 2)",
      "verdict": "BLOCK",
      "mode": "HARD",
      "reason": "PRIMARY HARD FINDING — stale guard resets on whitespace-only raw lines, directly contradicting SEAMS §2.\n\nsrc/providers/claudeCliClient.ts:692-701: The condition `if (line.length > 0) { yield line; yieldedNonEmpty = true; }` fires for any raw line with a non-zero byte count. The T-stale test lines '   ' (length 3), ' \\t ' (length 3), and '  ' (length 2) all pass this check, so `stale.reset()` is called three times in T-stale. Confirmed by direct execution: stale guard reset 3 times due to whitespace lines; spec says 0.\n\nSEAMS §2 states: \"T2 STALE-STREAM timeout (staleStreamMs): resets only when a NON-EMPTY NDJSON line is actually yielded (real progress). Catches the trickle-whitespace / keepalive-but-no-progress hang that the idle timer misses.\" The step->verify chain for T-stale (step 5) claims \"stream trickles empty/whitespace lines forever (resets T1 but never yields a non-empty NDJSON line)\". That claim is false: whitespace lines with length > 0 ARE yielded and DO reset T2.\n\nConsequence: in production, a process continuously trickling whitespace-padded newlines (e.g., '   \\n') would reset the stale timer indefinitely. Neither T1 nor T2 would ever fire. The UI hangs forever — the exact failure mode the stale guard was designed to prevent.\n\ntests/claudeCliClient.test.ts:845-874: The T-stale test does not verify the stated mechanism. clock.fire((t) => t.ms === staleStreamMs) fires the stale timer that was re-armed after the last whitespace line, because the stream then hangs (hangForever). The test would pass identically regardless of whether whitespace lines reset T2 — it only verifies whitespace-then-hang triggers the stale timer, not that continuous whitespace alone triggers it.\n\nADVISORY (secondary): Missing it.return?.() in the finally block at claudeCliClient.ts:710-714 — the hung async iterator is abandoned on abort/stall. Self-heals in production when the child is killed; dangling promise in synthetic tests only.\n\nFIX: Change the stale reset condition from raw `line.length > 0` to `parseJsonObject(line) !== undefined`. Only valid NDJSON events constitute real progress per the SEAMS. Update T-stale to send continuous whitespace (no eventual hang) and verify the stale timer fires.",
      "citation": "src/providers/claudeCliClient.ts:692-701 (yieldedNonEmpty set for any line.length > 0, including whitespace); tests/claudeCliClient.test.ts:845-874 (T-stale test does not assert whitespace fails to reset T2, only that stale fires after whitespace+hang)"
    },
    {
      "judge": "assumptions-assay",
      "verdict": "BLOCK",
      "mode": "HARD",
      "reason": "Silent, undeclared semantic assumption defeats the feature's named threat. claudeCliClient.ts:692,:700 resets the STALE guard on ANY non-empty raw line (line.length>0 -> yieldedNonEmpty -> stale.reset()), including whitespace-only and unparseable garbage lines. This contradicts the code's own docstring (claudeCliClient.ts:51-55: 'resets only when a NON-EMPTY parsed NDJSON line ... real progress ... Catches the trickle-whitespace / keepalive-but-no-progress hang') and SEAMS §2 T2 (same wording). Consequence: a newline-terminated whitespace/keepalive trickle resets BOTH idle (:683, every chunk) and stale (:700, every non-empty line), so neither timer ever fires and the UI hangs forever — exactly the keepalive-but-no-progress case T2 exists to catch. The pump has no parse step, so 'parsed NDJSON line (real progress)' was silently narrowed to 'any non-empty raw line'. The green gate hides this: T-stale (tests/claudeCliClient.test.ts:845-874) fires the stale timer manually via the fake clock (:863) instead of exercising the reset path under a continuous trickle, and its comment (:848-849, 'make no real progress') is false for this implementation — the whitespace lines ARE yielded and DO reset stale. Mode HARD: this nullifies the feature against the threat the SEAMS named and diverges from the documented contract; per remit I default to BLOCK when the load-bearing claim ('Catches the trickle-whitespace hang') cannot be verified, and it is verifiably not caught for the newline-terminated case. Fix: gate yieldedNonEmpty on line.trim().length>0 (or on successful parse).",
      "citation": "src/providers/claudeCliClient.ts:700"
    },
    {
      "judge": "complexity-assay",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "Architecture is clean and minimal: makeGuard is a non-redundant abstraction, PumpRace narrows correctly, StreamStallError is not exported, both timers clear unconditionally in finally (claudeCliClient.ts:710-714), frozen seams are confirmed untouched (empty diff on events.ts/contracts.ts/reducer.ts), worktree isolation is correct, gate is 335/335 tsc 0.\n\nPRIMARY FINDING — stale guard does not implement the spec's whitespace-trickle protection (claudeCliClient.ts:692-700): yieldedNonEmpty is set true when line.length > 0, which includes whitespace-only lines such as '   '. stale.reset() fires on these lines, contradicting SEAMS §2 (\"resets only when a NON-EMPTY parsed NDJSON line is actually yielded\"). An infinite whitespace trickle would reset both timers continuously; neither fires; the process hangs forever — exactly the scenario T2 was specified to guard. T-stale (tests/claudeCliClient.test.ts:851) covers a finite whitespace sequence followed by hangForever, not continuous whitespace trickle, so the test passes for the wrong reason and provides false confidence. Fix: change line 692 from `if (line.length > 0)` to `if (line.trim().length > 0)` and add a T-stale-infinite variant that fires the stale timer while whitespace chunks are still arriving.\n\nSECONDARY (negligible): abort event listener at claudeCliClient.ts:655 stays on the signal on normal exit until GC ({once:true} removes it on abort-fires but not on normal return); flush() at tests/claudeCliClient.test.ts:266 relies on 3x setTimeout(0) microtask ordering, which is sufficient but brittle.\n\nVerdict is PASS because real-world risk is low (claude -p emits structured NDJSON, not infinite whitespace), the dominant hang case (complete silence) is correctly caught by T1, and the fix is one character. The stale coverage gap should be patched before merge.",
      "citation": "claudeCliClient.ts:692-700 (stale reset on whitespace); tests/claudeCliClient.test.ts:851-863 (T-stale finite coverage gap)"
    },
    {
      "judge": "Scope Assay — forge/streaming-health-checks",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All 401 inserted lines trace to SEAMS spec items. Frozen seams (events.ts, contracts.ts, reducer.ts) are completely untouched (empty diff). No new AgentEvent variants, no new StopReason, no public contract changes. Two ADVISORY findings:\n\nADVISORY-1 (claudeCliClient.ts:34): TimerHandle is exported as a public named interface. SEAMS §1(a) describes the setTimer? return as an inline type { clear: () => void }. The export is additive, harmless, and the test file does not even import it (the fake clock at test:228 uses the structurally identical inline form). No behavioral impact.\n\nADVISORY-2 (claudeCliClient.ts:692-694, 700-701): Stale guard resets when line.length > 0, which includes whitespace strings such as '   '. SEAMS §2 specifies stale resets only on a NON-EMPTY PARSED NDJSON line (real progress). A server that streams infinite whitespace keepalives (never valid JSON) would keep resetting the stale guard indefinitely and it would never fire — defeating the stated purpose of T2. T-stale (test:845) covers whitespace-then-hangForever but not pure infinite-whitespace-trickle. The implementation comment at :699 documents the weaker condition. The gap materializes only in an edge case unlikely from claude -p; the feature is a strict improvement over no timeout at all. Spec-correct fix would be to move stale.reset() to the outer for-await loop, after parseJsonObject succeeds.\n\nNo HARD violations. Gate verified (tsc 0, vitest 335/335, commit 3d0da28 on forge/streaming-health-checks, main untouched).",
      "citation": "ADVISORY-1: src/providers/claudeCliClient.ts:34 (export interface TimerHandle) vs SEAMS §1(a) inline return type. ADVISORY-2: src/providers/claudeCliClient.ts:692-694,700-701 (yieldedNonEmpty = line.length > 0; stale.reset()) vs SEAMS §2 'NON-EMPTY PARSED NDJSON line'; T-stale at tests/claudeCliClient.test.ts:845 covers whitespace+hangForever but not infinite-whitespace-trickle."
    },
    {
      "judge": "Assay (step->verify chain + empty-diff guard)",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "All 7 step->verify clauses independently verified against the worktree at C:/Users/Core/src/juno-forge-streaming-health-checks, and the objective gate was re-run (not self-reported). Step 0: git worktree list confirms branch forge/streaming-health-checks @3d0da28, main untouched @6746127. Step 1: frozen seams provably untouched — `git diff main...forge -- src/core/contracts.ts src/core/events.ts src/core/reducer.ts` is empty; stall surfaced via existing {type:'error'} + assistant-done('error') at claudeCliClient.ts:305-306. Step 2: optional deps idleTimeoutMs/staleStreamMs/setTimer at claudeCliClient.ts:50,57,62 + file-local StreamStallError :84-91. Step 3: readLinesWithTimeout :612 manually drives stdout[Symbol.asyncIterator]() :619, races next vs idle/stale/abort :665, idle.reset on every chunk :683, stale.reset only on non-empty yield :700-702, newline-split verbatim :687-709, both timers cleared in finally :710-715. Step 4: onStall kill+throw :171-179, catch yields error+assistant-done('error') abort-wins :301-307; T-idle/T-stale green with killCount===1 and 'idle'/'stale' in message. Step 5: T-no-false-positive (killCount 0, pending empty), T-abort-beats-stall ({aborted}, no error, cleared), T-timers-cleared (>=2 armed, all cleared) all green. Step 6 re-run by me: npx tsc --noEmit exit 0; npx vitest run 335/335 passed across 23 files, claudeCliClient.test.ts 28 tests (23+5). Empty-diff guard PASS (2 files, 401 insertions). No discrepancy between self-report and tree.",
      "citation": "claudeCliClient.ts:612-716 (readLinesWithTimeout), :171-179 (onStall), :298-308 (catch surfaces stall as error+assistant-done); tests/claudeCliClient.test.ts:810-940 (5 health-check cases); gate: tsc exit 0, vitest 335/335"
    },
    {
      "judge": "architecture-assay",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "FROZEN-SEAM COMPLIANCE — VERIFIED CLEAN. `git diff --name-only main...forge/streaming-health-checks` shows exactly two files touched: src/providers/claudeCliClient.ts and tests/claudeCliClient.test.ts. None of the three Constitution-I.3 frozen files were modified, confirmed by their absence from the change set. The frozen surfaces the feature depends on all exist unchanged and provide what the diff relies on: src/core/events.ts:22 StopReason includes 'error'; events.ts:43 `{type:'assistant-done';id;stopReason:StopReason}`, events.ts:45 `{type:'aborted'}`, events.ts:46 `{type:'error';message:string}`; src/core/contracts.ts:96 `streamTurn(input,tools,signal):AsyncIterable<AgentEvent>` signature is byte-for-byte the original. The stall therefore surfaces via EXISTING variants only — no new AgentEvent member, no contract change, escalate=false is substantiated.\n\nCOMPOSITION WITH JUNO SEAMS — VERIFIED. The stall path composes through the pre-existing catch (claudeCliClient.ts:298-308): on throw it removes the abort listener, and at :301 `if(signal.aborted)` yields `{aborted}` FIRST (abort-wins precedence preserved), else :305-306 yields `{error}` then `{assistant-done stopReason:'error'}` — exactly the render-path the reducer/UI already handle. Render-only invariant intact: cliStopReason (:727) is unchanged, so a stall maps to 'error', never 'tool_use' (no spurious tool re-exec). New ClaudeCliDeps fields (idleTimeoutMs?/staleStreamMs?/setTimer?) are file-local and OPTIONAL with production defaults 60_000/90_000 wrapping real setTimeout — zero caller breakage; providers/index.ts correctly left untouched (spec §1 marks it optional). StreamStallError is file-local and not exported (an internal control signal). Resource bound verified: makeGuard timers are cleared on every exit path in the `finally` (:710-715); onStall (:171-179) kills the child before throwing. Pump (readLinesWithTimeout :612-716) preserves the original newline-split/\\r-strip/trailing-tail logic verbatim, resets idle on every chunk (:683) and stale only on a non-empty yielded line (:700-701), and the abort race (:651-657,:667-669) returns so the caller's signal.aborted paths win.\n\nOBJECTIVE GATE — RE-RUN, NOT SELF-REPORTED. `npx tsc --noEmit` exit 0. `npx vitest run` = 335 passed / 335 (23 files), matching the claimed baseline+5. Target test file = 28 tests green including the 5 new streaming-health cases. Branch confirmed forge/streaming-health-checks @3d0da28 via worktree; main untouched @6746127. Every step->verify claim independently reproduced. No findings; nothing to block.",
      "citation": "src/core/events.ts:22,43,45,46; src/core/contracts.ts:96; src/providers/claudeCliClient.ts:171-179,197-202,298-308,612-716,727; tsc=0; vitest 335/335"
    }
  ],
  "advisories": [
    {
      "judge": "complexity-assay",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "Architecture is clean and minimal: makeGuard is a non-redundant abstraction, PumpRace narrows correctly, StreamStallError is not exported, both timers clear unconditionally in finally (claudeCliClient.ts:710-714), frozen seams are confirmed untouched (empty diff on events.ts/contracts.ts/reducer.ts), worktree isolation is correct, gate is 335/335 tsc 0.\n\nPRIMARY FINDING — stale guard does not implement the spec's whitespace-trickle protection (claudeCliClient.ts:692-700): yieldedNonEmpty is set true when line.length > 0, which includes whitespace-only lines such as '   '. stale.reset() fires on these lines, contradicting SEAMS §2 (\"resets only when a NON-EMPTY parsed NDJSON line is actually yielded\"). An infinite whitespace trickle would reset both timers continuously; neither fires; the process hangs forever — exactly the scenario T2 was specified to guard. T-stale (tests/claudeCliClient.test.ts:851) covers a finite whitespace sequence followed by hangForever, not continuous whitespace trickle, so the test passes for the wrong reason and provides false confidence. Fix: change line 692 from `if (line.length > 0)` to `if (line.trim().length > 0)` and add a T-stale-infinite variant that fires the stale timer while whitespace chunks are still arriving.\n\nSECONDARY (negligible): abort event listener at claudeCliClient.ts:655 stays on the signal on normal exit until GC ({once:true} removes it on abort-fires but not on normal return); flush() at tests/claudeCliClient.test.ts:266 relies on 3x setTimeout(0) microtask ordering, which is sufficient but brittle.\n\nVerdict is PASS because real-world risk is low (claude -p emits structured NDJSON, not infinite whitespace), the dominant hang case (complete silence) is correctly caught by T1, and the fix is one character. The stale coverage gap should be patched before merge.",
      "citation": "claudeCliClient.ts:692-700 (stale reset on whitespace); tests/claudeCliClient.test.ts:851-863 (T-stale finite coverage gap)"
    },
    {
      "judge": "Scope Assay — forge/streaming-health-checks",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All 401 inserted lines trace to SEAMS spec items. Frozen seams (events.ts, contracts.ts, reducer.ts) are completely untouched (empty diff). No new AgentEvent variants, no new StopReason, no public contract changes. Two ADVISORY findings:\n\nADVISORY-1 (claudeCliClient.ts:34): TimerHandle is exported as a public named interface. SEAMS §1(a) describes the setTimer? return as an inline type { clear: () => void }. The export is additive, harmless, and the test file does not even import it (the fake clock at test:228 uses the structurally identical inline form). No behavioral impact.\n\nADVISORY-2 (claudeCliClient.ts:692-694, 700-701): Stale guard resets when line.length > 0, which includes whitespace strings such as '   '. SEAMS §2 specifies stale resets only on a NON-EMPTY PARSED NDJSON line (real progress). A server that streams infinite whitespace keepalives (never valid JSON) would keep resetting the stale guard indefinitely and it would never fire — defeating the stated purpose of T2. T-stale (test:845) covers whitespace-then-hangForever but not pure infinite-whitespace-trickle. The implementation comment at :699 documents the weaker condition. The gap materializes only in an edge case unlikely from claude -p; the feature is a strict improvement over no timeout at all. Spec-correct fix would be to move stale.reset() to the outer for-await loop, after parseJsonObject succeeds.\n\nNo HARD violations. Gate verified (tsc 0, vitest 335/335, commit 3d0da28 on forge/streaming-health-checks, main untouched).",
      "citation": "ADVISORY-1: src/providers/claudeCliClient.ts:34 (export interface TimerHandle) vs SEAMS §1(a) inline return type. ADVISORY-2: src/providers/claudeCliClient.ts:692-694,700-701 (yieldedNonEmpty = line.length > 0; stale.reset()) vs SEAMS §2 'NON-EMPTY PARSED NDJSON line'; T-stale at tests/claudeCliClient.test.ts:845 covers whitespace+hangForever but not infinite-whitespace-trickle."
    }
  ]
}
