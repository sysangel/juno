{
  "n": 7,
  "item": {
    "title": "byte-stable prompt + ephemeral injection",
    "gap": "buildRequestBody() in anthropicClient.ts rebuilds the system block from TurnInput.systemPrompt on every streamTurn call. Volatile data (memory, per-turn context) is mixed into the stable tier rather than the user message. No cache_control breakpoints are emitted. Consequence: Anthropic's server-side prompt cache never warms — the prefix mutates every call — so every turn pays full input-token cost. The Hermes doc (section 4) documents the pattern and the ~2-3x cost/latency win explicitly."
  },
  "outcome": "merged",
  "branch": "forge/byte-stable-prompt-ephemeral-injection",
  "writerPath": "triad",
  "verdicts": [
    {
      "judge": "correctness",
      "verdict": "BLOCK",
      "mode": "HARD",
      "reason": "One hard correctness regression plus one advisory gap.\n\nHARD — consecutive user-role messages break the Anthropic API\n\nsrc/providers/anthropicClient.ts:238 now maps ALL of input.messages through toAnthropicMessage in original order. toAnthropicMessage (anthropicClient.ts:284-287) maps role:'system' → {role:'user'}. Two documented code paths in the existing codebase produce a role:'system' message in committed that is immediately followed by a role:'user' message in committed:\n\n1. Compaction (src/core/reducer.ts:346-359): committed = [summaryMsg(system), ...keep]. chooseKeepCount (src/agent/compactor.ts:129-131) stops on a user boundary, so the kept slice always starts with a user message. After compaction: committed[0].role='system', committed[1].role='user'. toTurnMessages (src/hooks/useStreamingTurn.ts:113-114) preserves both; wire body gets [{role:'user'}, {role:'user'}, ...] — two consecutive user messages.\n\n2. Error recovery (src/core/reducer.ts:314-323): an error appends a role:'system' message to committed (phase → error). submit() is gated on controllerRef.current !== null (useStreamingTurn.ts:381), not on phase === error, so a user can type and submit after an error. That user submission dispatches user-submit which appends a role:'user' to committed. On the next turn: [..., error-system-msg, user-msg] → wire [..., {role:'user'}, {role:'user'}].\n\nAnthropic's messages API requires strictly alternating user/assistant turns; consecutive same-role entries return 400. On main, these system-role messages were extracted before calling toAnthropicMessage (old lines 229-235 split messages by role and placed system-content into body.system as a string) — so they never reached body.messages. The new code removes that partition and the problem appears.\n\nThe tests pass because fakeFetch does not validate Anthropic API role-alternation rules. SEAMS §3b acknowledges \"A leading system→user message is valid\" but never addresses mid-conversation adjacency. The Anthropic 400 path exists in production and is not scoped away.\n\nFix required before merge: either (a) merge consecutive user-role entries into a single content-array user block in buildRequestBody, or (b) re-introduce a partition that merges volatile system content into the preceding or following user block rather than emitting it as a standalone user message, preserving the stable/volatile separation but maintaining alternating roles.\n\nADVISORY — SEAMS §3c trailing message breakpoint not implemented and not tagged\n\nSEAMS §3c explicitly permits shipping without the trailing message breakpoint (\"if it complicates the diff, ship 3a+3b alone and leave 3c as a tagged follow-up\"). The implementation ships 3a+3b only — that is allowed. However, SEAMS says \"tagged follow-up\" and there is no TODO/DEFER comment in src/providers/anthropicClient.ts marking the omission. This is advisory: the dominant cache win (system/tools prefix) is in place; the missing comment is a maintenance debt, not a correctness bug.\n\nFrozen-seam check: git diff on src/core/contracts.ts, src/core/events.ts, src/core/reducer.ts is empty — PASS.\nGate: tsc --noEmit = 0, vitest 379/379 — PASS.",
      "citation": "src/providers/anthropicClient.ts:238 (messages: input.messages.map(toAnthropicMessage)); src/providers/anthropicClient.ts:283-287 (toAnthropicMessage system→user mapping); src/core/reducer.ts:346-359 (compaction produces [system, user, ...]); src/core/reducer.ts:314-323 (error appends system to committed); src/hooks/useStreamingTurn.ts:113-114 (toTurnMessages preserves system role); src/hooks/useStreamingTurn.ts:381 (submit guard is controllerRef only, not phase guard)"
    },
    {
      "judge": "assumptions",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "Scanned the authoritative diff (commit 21de26d), the implemented file, and the frozen-seam state against the SEAMS spec for undeclared scope decisions.\n\nVERIFIED CLEAN:\n- Frozen seams byte-unchanged: `git diff --stat main...branch -- src/core/contracts.ts src/core/events.ts src/core/reducer.ts` returns EMPTY. §5.6 met, escalate=false honored.\n- §3d hard constraint respected: no mid-conversation role:'system' introduced. toAnthropicMessage (anthropicClient.ts:285-287) folds system->user, so volatile content rides the user channel after the cached prefix. No Opus-4.8-only path.\n- §3a met: body.system is [{type:'text',text,cache_control:{type:'ephemeral'}}] (anthropicClient.ts:244-245); omitted when empty/undefined.\n- §3b met: all input.messages mapped in original order (anthropicClient.ts:238); the system/conversation partition is deleted; no role==='system' extraction into body.system remains.\n- No dead import: TurnMessage still used at anthropicClient.ts:283.\n- Regression test genuinely exercises the bug: baseInput (tests:12-15, one user msg) vs turn-2 with same systemPrompt + appended {role:'system'} entry, asserting JSON.stringify equality of body.system. Acceptance §5.3/§5.4/§5.5 covered by the four added/edited tests.\n\nONE ADVISORY FINDING (undeclared scope reduction):\n- anthropicClient.ts:226-257 — §3c (multi-turn trailing-message ephemeral breakpoint) was omitted. The SEAMS §3c is labeled \"Recommended, bounded\" and explicitly permits shipping 3a+3b alone, BUT requires that if 3c is dropped it be \"leave[n] 3c as a tagged follow-up.\" There is NO follow-up tag/note in the diff or in the commit body (commit 21de26d documents only 3a/3b). This is a spec-SANCTIONED scope reduction whose only deficiency is the missing explicit deferral marker — documentation hygiene, not a correctness or contract break. Step 5d (\"marker on the last SYSTEM block\") is independently satisfied by the single system block; 5d did not require the 3c message-level breakpoint, so there is no silent reinterpretation of the verify chain.\n\nMode is ADVISORY (declared explicitly, not a silent degradation): the lone finding is an optional, spec-permitted omission lacking only a follow-up tag; every HARD acceptance criterion (§5.2-§5.6) is verifiable from the diff/tree and is met. No unverifiable claim forces a default BLOCK — gate counts (379/379) are orchestrator-run per Step 7, not relied upon as agent-self-report for this verdict.",
      "citation": "src/providers/anthropicClient.ts:226-257 (3c omission, no deferral tag); :238 (all messages mapped); :244-245 (cache-marked block); :285-287 (system->user fold, §3d honored); :283 (TurnMessage live); tests/modelClients.fake.test.ts:12-15 (baseInput), :375-476 (edited+added tests); git diff --stat of contracts.ts/events.ts/reducer.ts = empty (§5.6); commit 21de26d body (no 3c follow-up note)"
    },
    {
      "judge": "complexity",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "All five acceptance criteria and all six step->verify gates pass.\n\nOBJECTIVE GATE (orchestrator-run):\n- tsc --noEmit: 0 errors (clean).\n- vitest full suite: 379/379 passed (baseline was ~330; delta is the 4 new tests plus pre-existing count growth). Relevant file 34/34.\n- build: no build script exists; npm run typecheck is the canonical gate and passes (exit 0).\n\nFROZEN-SEAM CHECK:\n- git diff main...HEAD -- src/core/contracts.ts src/core/events.ts src/core/reducer.ts: empty output. All three byte-unchanged. SEAMS §0 satisfied.\n\nIMPLEMENTATION MINIMALITY (senior-engineer inversion test):\n- src/providers/anthropicClient.ts:226-246: The old 9-line system/conversation partition loop is deleted entirely; replaced by 3 lines: one message map and one conditional block assignment. No residual branching, no new helpers, no new types.\n- The volatile mapping (role:'system' → role:'user') was already correct in toAnthropicMessage (lines 285-287) before this change; the fix simply stops preventing it from running by removing the extraction step that hoisted those messages out of input.messages before the map. Zero new mapping code required.\n- No scope creep: claudeCliClient.ts and openaiCompatClient.ts are untouched (verified via diff --stat showing exactly 2 files changed).\n- SEAMS §3c (trailing message breakpoint) is not implemented; the spec explicitly permits shipping §3a+3b alone if §3c complicates the diff, and tags it as a follow-up. One breakpoint emitted (well within the 4-max constraint).\n\nTEST COVERAGE vs. SEAMS AC:\n- AC #2 (body.system is array with cache_control): tests/modelClients.fake.test.ts:390-392.\n- AC #3 (byte-identical body.system across turns with different volatile system messages): tests/modelClients.fake.test.ts:419-424 — JSON.stringify equality asserted on captured[0] vs captured[1].\n- AC #4 (system-role in messages → user-role in body.messages, absent from body.system): tests/modelClients.fake.test.ts:449-457.\n- AC #5 (systemPrompt undefined/empty → body.system absent): tests/modelClients.fake.test.ts:468-472, using Object.hasOwn for both undefined and empty-string cases.\n- AC #6 (frozen seams unchanged): confirmed above.\n\nThe anthropicEndTurnChunks() helper introduced at tests/modelClients.fake.test.ts:123-129 is a legitimate deduplication of a two-event sequence that appears four times across the new tests — not padding.\n\nNo findings requiring BLOCK. Implementation is minimal, correct, and within spec.",
      "citation": "src/providers/anthropicClient.ts:226-246 (core change); tests/modelClients.fake.test.ts:378-473 (new + updated tests); git diff --stat (frozen-seam files absent from diff)"
    },
    {
      "judge": "scope",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All six SEAMS acceptance criteria verified.\n\nAC1 — OBJECTIVE GATE: tsc --noEmit = 0 errors; vitest 379/379 (baseline 376, net +3 new tests). `npm run build` does not exist in this project on either main or the branch (tsx-run project; no build artifact step). SEAMS AC1 references \"build green\" but the gate is constitutionally tsc+vitest; both are green. ADVISORY: the SEAMS should be corrected to drop the build-script reference for this project, but this is a spec-authoring gap, not a code defect.\n\nAC2 — anthropicClient.ts:244-246: `body.system = [{ type: 'text', text: input.systemPrompt, cache_control: { type: 'ephemeral' } }]` — structured block array with ephemeral marker confirmed.\n\nAC3 — tests/modelClients.fake.test.ts:395-425: byte-stability regression test drains twice with identical systemPrompt but second call carries an appended {role:'system'} message; asserts JSON.stringify(firstSystem) === JSON.stringify(secondSystem). Confirmed.\n\nAC4 — tests/modelClients.fake.test.ts:427-458 + anthropicClient.ts:283-287: role:'system' in input.messages maps to {role:'user'} in body.messages via the existing toAnthropicMessage case-fall-through (lines 285-287). body.system does not contain volatile content. Confirmed.\n\nAC5 — tests/modelClients.fake.test.ts:460-473: both undefined systemPrompt (baseInput) and systemPrompt:'' checked with Object.hasOwn; anthropicClient.ts:244 condition `input.systemPrompt !== undefined && input.systemPrompt.length > 0` guards the omit. Confirmed.\n\nAC6 — FROZEN SEAMS: `git diff main...forge/byte-stable-prompt-ephemeral-injection -- src/core/contracts.ts src/core/events.ts src/core/reducer.ts` produces empty output. Only src/providers/anthropicClient.ts and tests/modelClients.fake.test.ts changed (diff --stat confirms). No scope creep into claudeCliClient.ts or openaiCompatClient.ts.\n\nSEAMS §3c (trailing-message breakpoint) — not implemented. The SEAMS explicitly provides an opt-out: \"if it complicates the diff, ship 3a+3b alone and leave 3c as a tagged follow-up.\" The implementer exercised this opt-out. ADVISORY: 3c should be filed as a follow-up.\n\nTest title change at tests/modelClients.fake.test.ts:378 from \"folds systemPrompt into the system field\" to \"emits systemPrompt as a cache-marked system block\" is more precise and accurately describes the new behavior. Not a violation.\n\nNo HARD findings. Two ADVISORY items: (1) SEAMS AC1 references a build script that does not exist in this project — spec text needs correction, not the code; (2) §3c trailing-message breakpoint deferred per spec opt-out — should be tagged as a follow-up work item.",
      "citation": "anthropicClient.ts:226-256 (buildRequestBody rewrite); anthropicClient.ts:283-287 (toAnthropicMessage role:'system' fold); tests/modelClients.fake.test.ts:378-473 (updated + 3 new tests); git diff --stat (only 2 files changed); frozen-seam diff (empty)"
    },
    {
      "judge": "goal",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "Diff is non-empty (empty-diff guard satisfied: 2 files, +107/-23). Every step->verify clause verified against the authoritative diff and a clean gate run in the worktree:\n\nStep 1 baseline / Step 7 gate: `npx tsc --noEmit` = exit 0; `npx vitest run` = 379/379 passed across 24 files (>= baseline ~330). (anthropicClient.ts gate run, not agent-reported.)\n\nStep 2/4 (pinned test) — tests/modelClients.fake.test.ts:393-397 (renamed `it('...emits systemPrompt as a cache-marked system block')`): the old `toBe('be terse')` string assertion is replaced by `expect(captured[0]?.body?.system).toEqual([{ type: 'text', text: 'be terse', cache_control: { type: 'ephemeral' } }])`. Matches AC #2; title still describes folding systemPrompt into system.\n\nStep 3 (core edit) — src/providers/anthropicClient.ts:240 `messages: input.messages.map(toAnthropicMessage)` (partition at old lines 227-235 deleted); anthropicClient.ts:246-248 sets `body.system = [{type:'text', text:input.systemPrompt, cache_control:{type:'ephemeral'}}]` only when `input.systemPrompt !== undefined && input.systemPrompt.length > 0`. No `role==='system'` extraction into body.system remains; no mid-conversation role:'system' introduced (confirmed §3d constraint respected — anthropicClient.ts:285-287 maps system->{role:'user'}).\n\nStep 5a byte-stability — tests/modelClients.fake.test.ts:418-446: `JSON.stringify(firstSystem)` === `JSON.stringify(secondSystem)` with an appended {role:'system'} in turn 2. PASS (AC #3).\nStep 5b volatile placement — tests/modelClients.fake.test.ts:448-485: `not.toContain('volatile transcript note')` on body.system AND body.messages asserts the system entry maps to {role:'user', content:'volatile transcript note'}. PASS (AC #4).\nStep 5c empty — tests/modelClients.fake.test.ts:487-503: undefined and '' systemPrompt both yield `Object.hasOwn(body,'system') === false`. PASS (AC #5).\nStep 5d breakpoint — asserted via the cache_control:{type:'ephemeral'} marker on the system block at tests/modelClients.fake.test.ts:395-396 (and 414-416, 466-468). PASS.\n\nStep 6 frozen-seam — `git diff --stat main...forge/... -- src/core/contracts.ts src/core/events.ts src/core/reducer.ts` returns EMPTY; all three byte-unchanged. PASS (AC #6).\n\nStep 8 live confirmation — deferred (no ANTHROPIC_API_KEY); Steps 5a/5d serve as the deterministic proxy per spec.\n\n§3c trailing-message breakpoint: intentionally NOT added (spec marked optional/\"ship 3a+3b alone\"); breakpoint count = 1 (<=2). Acceptable.",
      "citation": "src/providers/anthropicClient.ts:240,246-248,285-287; tests/modelClients.fake.test.ts:393-397,418-446,448-485,487-503; git diff --stat (contracts.ts/events.ts/reducer.ts empty)"
    },
    {
      "judge": "architecture",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "Frozen-seam compliance and Juno-seam composition both verified from the diff and a tree inspection. (1) FROZEN SEAMS CLEAN: git --name-only/--stat show only src/providers/anthropicClient.ts and tests/modelClients.fake.test.ts changed; contracts.ts, events.ts, reducer.ts byte-unchanged (AC #6, §0 escalate=false confirmed). buildRequestBody keeps its (entry,input,tools)->JsonObject signature; body.system is a local Anthropic-wire field, not a Juno contract. (2) §3a SATISFIED at anthropicClient.ts:243-245 — body.system = [{type:'text', text:input.systemPrompt, cache_control:{type:'ephemeral'}}] gated on systemPrompt !== undefined && length>0, matching AC #2 and the prompt-caching reference (only the array form carries cache_control; marker on the sole system block caches tools+system together, render order tools->system). (3) §3b SATISFIED — system/conversation partition deleted; messages: input.messages.map(toAnthropicMessage) in original order; toAnthropicMessage at anthropicClient.ts:283-286 folds role:'system' -> {role:'user',content}, landing volatile content AFTER the cached prefix (the load-bearing byte-stability fix). (4) §3d HONORED — uses the user-role path, NOT the Opus-4.8-only mid-conversation role:'system' channel, so no 400 on the claude-sonnet-4-6 catalog default. (5) §3c trailing breakpoint correctly omitted (spec permitted 3a+3b alone); single breakpoint, no 20-block-lookback issue. TESTS: AC #3 byte-stability regression asserts JSON.stringify(firstSystem)===secondSystem across an appended role:'system' msg (correct deterministic proxy; live cache_read check deferred per §8); AC #4 asserts volatile content absent from body.system and present as a user-role message in order; AC #5 exercises both undefined (baseInput has no systemPrompt) and '' omission branches via Object.hasOwn. ADVISORY note (does not affect verdict): AC #1/#7 objective gate (tsc 0 / full vitest / build green) is the orchestrator's to run per Constitution I.5 and cannot be confirmed from the diff alone — outside this Assay's remit, flagged for the orchestrator to verify.",
      "citation": "src/providers/anthropicClient.ts:226-246 (buildRequestBody: all-messages map + cache-marked system array); src/providers/anthropicClient.ts:283-286 (toAnthropicMessage system->user fold); tests/modelClients.fake.test.ts:393-433 (byte-stability + volatile-placement + empty-omission regression tests); git diff main...forge/byte-stable-prompt-ephemeral-injection --name-only (only 2 files; no contracts/events/reducer)"
    },
    {
      "judge": "correctness",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All SEAMS acceptance criteria verified. tsc --noEmit = 0 errors; vitest 380/380 (up from ~330 baseline, 5 new/updated Anthropic tests); no build script exists in this project (tsc IS the type gate). Frozen-seam files (contracts.ts, events.ts, reducer.ts) are byte-unchanged per git diff --stat. Detailed findings:\n\nCORRECTNESS FINDINGS (all ADVISORY — no blockers):\n\n1. anthropicClient.ts:310 — mergeConsecutiveUserMessages mutates the pushed JsonObject's `content` field in-place. Safe: toAnthropicMessage always returns fresh objects with no back-aliases into TurnInput.messages. ADVISORY.\n\n2. The lone user entry (content:'first') in the alternation test (tests/modelClients.fake.test.ts:497) stays as a string, not promoted to a block array, because it is never merged (next entry is assistant). The Anthropic API accepts both string and block-array for user content, so this is correct wire behaviour. ADVISORY.\n\n3. SEAMS §3c (trailing-message cache breakpoint) is explicitly deferred with a tagged comment at anthropicClient.ts:241-246. The SEAMS explicitly permits shipping §3a+§3b alone; §3c is not an acceptance-criteria item. ADVISORY debt, not a correctness gap.\n\n4. No test covers the case where systemPrompt itself changes between turns (which would legitimately invalidate the cache). Not a defect — it is the expected and correct behaviour. ADVISORY.\n\nALL SEAMS ACCEPTANCE CRITERIA MET:\nAC1: tsc 0/vitest 380 green — verified by objective gate run.\nAC2: body.system = [{type:'text', text:systemPrompt, cache_control:{type:'ephemeral'}}] — verified at anthropicClient.ts:252 and tests/modelClients.fake.test.ts:390-392.\nAC3: Byte-stability regression — verified at tests/modelClients.fake.test.ts:395-425 (JSON.stringify equality assertion on body.system across two turns where only volatile transcript content differs).\nAC4: role:'system' in messages → {role:'user'} in body.messages, absent from body.system — verified at tests/modelClients.fake.test.ts:427-467.\nAC5: systemPrompt undefined/empty → body.system absent — verified at tests/modelClients.fake.test.ts:510-523 (Object.hasOwn check for both cases).\nAC6: Frozen-seam files untouched — verified by git diff --stat returning empty for contracts.ts, events.ts, reducer.ts.",
      "citation": "anthropicClient.ts:226-264 (buildRequestBody rewrite), anthropicClient.ts:304-317 (mergeConsecutiveUserMessages), anthropicClient.ts:325-333 (toContentBlocks), tests/modelClients.fake.test.ts:378-523 (5 Anthropic cache tests)"
    }
  ],
  "advisories": [
    {
      "judge": "assumptions",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "Scanned the authoritative diff (commit 21de26d), the implemented file, and the frozen-seam state against the SEAMS spec for undeclared scope decisions.\n\nVERIFIED CLEAN:\n- Frozen seams byte-unchanged: `git diff --stat main...branch -- src/core/contracts.ts src/core/events.ts src/core/reducer.ts` returns EMPTY. §5.6 met, escalate=false honored.\n- §3d hard constraint respected: no mid-conversation role:'system' introduced. toAnthropicMessage (anthropicClient.ts:285-287) folds system->user, so volatile content rides the user channel after the cached prefix. No Opus-4.8-only path.\n- §3a met: body.system is [{type:'text',text,cache_control:{type:'ephemeral'}}] (anthropicClient.ts:244-245); omitted when empty/undefined.\n- §3b met: all input.messages mapped in original order (anthropicClient.ts:238); the system/conversation partition is deleted; no role==='system' extraction into body.system remains.\n- No dead import: TurnMessage still used at anthropicClient.ts:283.\n- Regression test genuinely exercises the bug: baseInput (tests:12-15, one user msg) vs turn-2 with same systemPrompt + appended {role:'system'} entry, asserting JSON.stringify equality of body.system. Acceptance §5.3/§5.4/§5.5 covered by the four added/edited tests.\n\nONE ADVISORY FINDING (undeclared scope reduction):\n- anthropicClient.ts:226-257 — §3c (multi-turn trailing-message ephemeral breakpoint) was omitted. The SEAMS §3c is labeled \"Recommended, bounded\" and explicitly permits shipping 3a+3b alone, BUT requires that if 3c is dropped it be \"leave[n] 3c as a tagged follow-up.\" There is NO follow-up tag/note in the diff or in the commit body (commit 21de26d documents only 3a/3b). This is a spec-SANCTIONED scope reduction whose only deficiency is the missing explicit deferral marker — documentation hygiene, not a correctness or contract break. Step 5d (\"marker on the last SYSTEM block\") is independently satisfied by the single system block; 5d did not require the 3c message-level breakpoint, so there is no silent reinterpretation of the verify chain.\n\nMode is ADVISORY (declared explicitly, not a silent degradation): the lone finding is an optional, spec-permitted omission lacking only a follow-up tag; every HARD acceptance criterion (§5.2-§5.6) is verifiable from the diff/tree and is met. No unverifiable claim forces a default BLOCK — gate counts (379/379) are orchestrator-run per Step 7, not relied upon as agent-self-report for this verdict.",
      "citation": "src/providers/anthropicClient.ts:226-257 (3c omission, no deferral tag); :238 (all messages mapped); :244-245 (cache-marked block); :285-287 (system->user fold, §3d honored); :283 (TurnMessage live); tests/modelClients.fake.test.ts:12-15 (baseInput), :375-476 (edited+added tests); git diff --stat of contracts.ts/events.ts/reducer.ts = empty (§5.6); commit 21de26d body (no 3c follow-up note)"
    },
    {
      "judge": "scope",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All six SEAMS acceptance criteria verified.\n\nAC1 — OBJECTIVE GATE: tsc --noEmit = 0 errors; vitest 379/379 (baseline 376, net +3 new tests). `npm run build` does not exist in this project on either main or the branch (tsx-run project; no build artifact step). SEAMS AC1 references \"build green\" but the gate is constitutionally tsc+vitest; both are green. ADVISORY: the SEAMS should be corrected to drop the build-script reference for this project, but this is a spec-authoring gap, not a code defect.\n\nAC2 — anthropicClient.ts:244-246: `body.system = [{ type: 'text', text: input.systemPrompt, cache_control: { type: 'ephemeral' } }]` — structured block array with ephemeral marker confirmed.\n\nAC3 — tests/modelClients.fake.test.ts:395-425: byte-stability regression test drains twice with identical systemPrompt but second call carries an appended {role:'system'} message; asserts JSON.stringify(firstSystem) === JSON.stringify(secondSystem). Confirmed.\n\nAC4 — tests/modelClients.fake.test.ts:427-458 + anthropicClient.ts:283-287: role:'system' in input.messages maps to {role:'user'} in body.messages via the existing toAnthropicMessage case-fall-through (lines 285-287). body.system does not contain volatile content. Confirmed.\n\nAC5 — tests/modelClients.fake.test.ts:460-473: both undefined systemPrompt (baseInput) and systemPrompt:'' checked with Object.hasOwn; anthropicClient.ts:244 condition `input.systemPrompt !== undefined && input.systemPrompt.length > 0` guards the omit. Confirmed.\n\nAC6 — FROZEN SEAMS: `git diff main...forge/byte-stable-prompt-ephemeral-injection -- src/core/contracts.ts src/core/events.ts src/core/reducer.ts` produces empty output. Only src/providers/anthropicClient.ts and tests/modelClients.fake.test.ts changed (diff --stat confirms). No scope creep into claudeCliClient.ts or openaiCompatClient.ts.\n\nSEAMS §3c (trailing-message breakpoint) — not implemented. The SEAMS explicitly provides an opt-out: \"if it complicates the diff, ship 3a+3b alone and leave 3c as a tagged follow-up.\" The implementer exercised this opt-out. ADVISORY: 3c should be filed as a follow-up.\n\nTest title change at tests/modelClients.fake.test.ts:378 from \"folds systemPrompt into the system field\" to \"emits systemPrompt as a cache-marked system block\" is more precise and accurately describes the new behavior. Not a violation.\n\nNo HARD findings. Two ADVISORY items: (1) SEAMS AC1 references a build script that does not exist in this project — spec text needs correction, not the code; (2) §3c trailing-message breakpoint deferred per spec opt-out — should be tagged as a follow-up work item.",
      "citation": "anthropicClient.ts:226-256 (buildRequestBody rewrite); anthropicClient.ts:283-287 (toAnthropicMessage role:'system' fold); tests/modelClients.fake.test.ts:378-473 (updated + 3 new tests); git diff --stat (only 2 files changed); frozen-seam diff (empty)"
    },
    {
      "judge": "correctness",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All SEAMS acceptance criteria verified. tsc --noEmit = 0 errors; vitest 380/380 (up from ~330 baseline, 5 new/updated Anthropic tests); no build script exists in this project (tsc IS the type gate). Frozen-seam files (contracts.ts, events.ts, reducer.ts) are byte-unchanged per git diff --stat. Detailed findings:\n\nCORRECTNESS FINDINGS (all ADVISORY — no blockers):\n\n1. anthropicClient.ts:310 — mergeConsecutiveUserMessages mutates the pushed JsonObject's `content` field in-place. Safe: toAnthropicMessage always returns fresh objects with no back-aliases into TurnInput.messages. ADVISORY.\n\n2. The lone user entry (content:'first') in the alternation test (tests/modelClients.fake.test.ts:497) stays as a string, not promoted to a block array, because it is never merged (next entry is assistant). The Anthropic API accepts both string and block-array for user content, so this is correct wire behaviour. ADVISORY.\n\n3. SEAMS §3c (trailing-message cache breakpoint) is explicitly deferred with a tagged comment at anthropicClient.ts:241-246. The SEAMS explicitly permits shipping §3a+§3b alone; §3c is not an acceptance-criteria item. ADVISORY debt, not a correctness gap.\n\n4. No test covers the case where systemPrompt itself changes between turns (which would legitimately invalidate the cache). Not a defect — it is the expected and correct behaviour. ADVISORY.\n\nALL SEAMS ACCEPTANCE CRITERIA MET:\nAC1: tsc 0/vitest 380 green — verified by objective gate run.\nAC2: body.system = [{type:'text', text:systemPrompt, cache_control:{type:'ephemeral'}}] — verified at anthropicClient.ts:252 and tests/modelClients.fake.test.ts:390-392.\nAC3: Byte-stability regression — verified at tests/modelClients.fake.test.ts:395-425 (JSON.stringify equality assertion on body.system across two turns where only volatile transcript content differs).\nAC4: role:'system' in messages → {role:'user'} in body.messages, absent from body.system — verified at tests/modelClients.fake.test.ts:427-467.\nAC5: systemPrompt undefined/empty → body.system absent — verified at tests/modelClients.fake.test.ts:510-523 (Object.hasOwn check for both cases).\nAC6: Frozen-seam files untouched — verified by git diff --stat returning empty for contracts.ts, events.ts, reducer.ts.",
      "citation": "anthropicClient.ts:226-264 (buildRequestBody rewrite), anthropicClient.ts:304-317 (mergeConsecutiveUserMessages), anthropicClient.ts:325-333 (toContentBlocks), tests/modelClients.fake.test.ts:378-523 (5 Anthropic cache tests)"
    }
  ]
}
