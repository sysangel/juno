{
  "n": 5,
  "item": {
    "title": "Iteration Budget + /steer Mid-Turn Inject",
    "gap": "turnRunner.ts has no per-turn tool-call ceiling: a runaway model can loop indefinitely until the provider's token limit or an OOM. No mid-turn guidance mechanism exists — the user must abort and restart to correct a drifting turn. These are the two named P0 robustness items remaining in TARGET_STATE after context-compression shipped in cycle 4."
  },
  "outcome": "merged",
  "branch": "forge/iteration-budget-steer-mid-turn-inject",
  "writerPath": "degraded",
  "verdicts": [
    {
      "judge": "correctness",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "No HARD issues found. Two ADVISORY findings only.\n\nADVISORY 1 — src/agent/turnRunner.ts:260-322 — Mid-batch budget over-run. The inner for-loop (line 260) runs all K tool calls in one assistant response to completion before the ceiling check fires at line 322. If K pushes the cumulative count past maxToolCalls, all K tools execute before detection. Primary guard use-case (one tool per response, runaway loop prevention) is fully correct; the over-run risk is bounded by tools-per-response and is consistent with the SEAMS (\"after tools have run\"), but is undocumented.\n\nADVISORY 2 — src/hooks/useStreamingTurn.ts:393 — setToolCallsThisTurn(0) is a React state setter (async re-render schedule). Between submit() starting and next repaint, the tools:N/max chip may briefly show the prior turn's count. Cosmetic and self-correcting on next frame.\n\nAll other checks pass: frozen files (events.ts, reducer.ts, contracts.ts) untouched; budget guard placement correct (after deferredToolUseDone dispatch + tool-status events, before steer drain, before currentInput mutation); breach-check-then-break ordered before steer drain per spec; double-exposure analysis clean (live loop reads from steerQueueRef, next submit reads from committed, queue cleared before runTurn starts); parseSteerArg word-boundary regex correctly rejects /steering; maxToolCalls absent from DEFAULT_SETTINGS; parseMaxToolCalls correctly rejects 0, negatives, NaN, Infinity, floats; new test count +27 verified (it.each generates 4 subtests); all test assertions are structurally sound against TurnMessage union and ScriptedClient.inputs capture pattern.",
      "citation": "turnRunner.ts:260-322 (mid-batch budget over-run); useStreamingTurn.ts:393 (async state reset); turnRunner.ts:294-328 (guard ordering); useStreamingTurn.ts:366-376 (steer double-exposure); app.tsx:87 (word-boundary regex); config.ts:48-66 (DEFAULT_SETTINGS); config.ts:172-175 (parseMaxToolCalls)"
    },
    {
      "judge": "assumptions",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "No undeclared scope decisions or silent assumptions found. Every assumption the SEAMS flagged is either enforced in code or explicitly declared in a comment. I verified the central risk (backend asymmetry) directly: claudeCliClient.ts:733 maps tool_use->'end' and turnRunner.ts:240 breaks on non-tool_use, so on claude-cli the executor loop, onIteration (turnRunner.ts:275-276), budget breach (turnRunner.ts:322), and drainSteer are all unreachable — the budget is inert and the tools: chip stays hidden by construction, exactly as SEAMS section 0/1 claim, with no misleading-UI assumption. The /steer overlay-open double-fire is handled, not assumed away: app.tsx:453-460 intercepts before the overlay-clear branch and before turn.submit, while runSlashCommand case 'steer' (app.tsx:350-355) only closes the overlay, so injection fires exactly once (declared at app.tsx:451-452). Reset/no-double-exposure logic in useStreamingTurn (submit resets toolCallsThisTurn + clears steerQueueRef; steer commits via user-submit; live loop reads queue not committed) matches SEAMS 2A. Frozen seams events.ts/reducer.ts/contracts.ts untouched. Advisory only: config.ts:29-36 doc omits the SEAMS 'Hermes default ~90' hint (correctly not added to DEFAULT_SETTINGS, unbounded preserved); config.mode.test.ts adds 9 cases vs prose '+8' due to it.each fan-out (gate total 376 is authoritative). Neither affects scope.",
      "citation": "src/providers/claudeCliClient.ts:733 (cliStopReason maps tool_use->end); src/agent/turnRunner.ts:240 (break on non-tool_use), :275-276 (onIteration), :322 (budget breach), :330-336 (drainSteer splice); src/app.tsx:350-355 (runSlashCommand case steer), :453-460 (submit /steer intercept ordering); src/hooks/useStreamingTurn.ts:386-400 (steer commit + reset); src/services/config.ts:29-36 (doc), :172-176 (parseMaxToolCalls), :358-366 (env override); src/ui/StatusLine.tsx:75-88 (chip guard used>0 && max!==undefined)"
    },
    {
      "judge": "complexity",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "Frozen seams confirmed clean (empty diff for events.ts, reducer.ts, contracts.ts). All changes are additive with optional fields. Budget counter (turnRunner.ts:96) is per-runTurn by construction. Breach check (turnRunner.ts:322-328) fires after the full tool batch commits and before currentInput update — no orphaned tool calls. Steer two-path design (steerQueueRef for live loop, user-submit dispatch for committed persistence) prevents double-injection within one turn and prevents loss across turns. Steer intercept (app.tsx:453-460) is correctly ordered before the generic slash branch. Config parser (config.ts:174-175) mirrors parseCompactionKeepBudget exactly. 27 new tests cover all seams including the load-bearing steer-position assertion (coordinator.test.ts:566). Solution is minimal: no new event variants, no reducer changes, no DEFAULT_SETTINGS entries. Two advisory findings: (1) budget check is per-response-batch not per-individual-tool (turnRunner.ts:260-322) — operators should set limit accounting for max parallel tools; (2) palette-selected /steer silently closes overlay with no UX feedback (app.tsx:350-355). Neither warrants a block.",
      "citation": "turnRunner.ts:96,322-328; useStreamingTurn.ts:181,366-376,394; app.tsx:350-355,453-460; config.ts:174-175; StatusLine.tsx:75-88; coordinator.test.ts:566"
    },
    {
      "judge": "scope",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "Every changed line in the diff traces to a concrete SEAMS item. Frozen seams (src/core/events.ts, src/core/reducer.ts, src/core/contracts.ts) are absent from the diff (verified via git diff --name-only). All 13 changed files implement exactly the spec'd additions: config seam (Settings.maxToolCalls + parseMaxToolCalls + env JUNO_MAX_TOOL_CALLS, SEAMS §1A); turnRunner seam (TurnRunnerDeps.maxToolCalls/onIteration/drainSteer, toolCallsThisTurn counter, breach check after commit before re-entry, steer splice after breach check, SEAMS §1B/§2A); hook seam (useState mirror, steerQueueRef, steer() callback, submit reset, SEAMS §1C/§2A); app seam (maxToolCalls thread, toolBudget surface, parseSteerArg, slashCommands entry, case 'steer', /steer interception before generic slash branch, SEAMS §1D/§2B); selectors seam (toolBudget field, SEAMS §1D); StatusLine chip (tools:used/max, warn at 80%, SEAMS §1D); all 7 test files cover exactly the SEAMS §3 named test seams. Ordering constraint honored: budget breach check (turnRunner.ts:322-328) precedes steer drain (turnRunner.ts:334) as required by SEAMS §2A. DEFAULT_SETTINGS untouched (unbounded default, SEAMS §1A). Deferred items (t:'steer' action, 'budget' StopReason, status hint) are correctly absent. One minor advisory finding: in app.tsx the interception block calls setValue('') (line 454) before parseSteerArg(nextValue) (line 455), inverted from the SEAMS pseudocode order, but nextValue is a local const and setValue is an async-state setter so there is zero behavioral difference — does not warrant a block.",
      "citation": "src/agent/turnRunner.ts:322-339 (budget check before steer splice); src/hooks/useStreamingTurn.ts:393-394 (submit reset); src/services/config.ts:174-176 (parseMaxToolCalls); src/app.tsx:453-460 (/steer interception); src/ui/StatusLine.tsx:75-88 (chip + 80% tint); git diff --name-only (frozen seams absent)"
    },
    {
      "judge": "goal",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "Ran the gate directly in the worktree at HEAD 39bbd05. Clause 1: `npx tsc --noEmit` exits 0 with no diagnostics. Clause 2: `npx vitest run` reports \"24 passed (24)\" / \"376 passed (376)\", exit 0 — matching the claimed 24 files, 376/376. Empty-diff guard satisfied: git diff main...HEAD is non-empty (13 files, +662/-2) and matches the authoritative DIFF. New-test count reconciles to +27 over the 349 baseline (coordinator+4, streamingTurn+3, slashIntercept+4, unifiedPalette+1, selectors+3, components+3, config.mode+9). The only nit is the step->verify prose labeling config.mode as \"+8\" when the `it.each([0,-1,1.5,NaN])` parametrization makes it +9; this is a cosmetic mislabel — the binding headline 376/376 is exact and verified green, so it does not block.",
      "citation": "tsc: TSC_EXIT=0 (no output); vitest: \"Test Files 24 passed (24)\" / \"Tests 376 passed (376)\" VITEST_EXIT=0; config.mode.test.ts it.each at tests/config.mode.test.ts (4-case parametrization) vs step->verify \"+8\"; turnRunner breach at src/agent/turnRunner.ts:320-325; steer splice at src/agent/turnRunner.ts:333-338"
    },
    {
      "judge": "architecture",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "Both features are fully additive and the three frozen seams are untouched — verified directly, not just from the diff narrative.\n\nFROZEN FILES UNTOUCHED (the core remit):\n- `git diff --stat main...branch -- src/core/events.ts src/core/reducer.ts src/core/contracts.ts` returns EMPTY. None of the three frozen files are in the changed-files list (only turnRunner, app, selectors, useStreamingTurn, config, StatusLine + 7 test files).\n\nSEAM-REUSE CLAIMS CONFIRMED (composes with existing Juno seams):\n- Budget breach reuses the EXISTING error event: src/core/events.ts:46 declares `{ type: 'error'; message: string }`, mapped at events.ts:83-84 to `{ t:'error', message }`. The new `dispatchEvent({ type:'error', ... })` at src/agent/turnRunner.ts:321-326 is a pre-existing event variant — no new event/StopReason added.\n- Terminal maps cleanly: src/core/reducer.ts:310-324 `case 'error'` sets `phase:'error'`, commits a system Msg, and sets `errorMessage` — so the breach is visible in transcript and the loop `break` falls through to the existing `finally` drain. No reducer change.\n- Steer message reuses an existing TurnMessage shape: src/core/contracts.ts:17 `{ role:'system'|'user'; content:string }`, so `steerMessages: { role:'user', content }` (turnRunner.ts) appends to the already-existing `TurnInput.messages` — no contracts field added.\n\nPLACEMENT/ORDERING CORRECT (verified turnRunner.ts:293-340): the budget check sits AFTER the tool loop and after assistantMessage/toolMessages are built (so in-flight tool results are preserved and committed, no orphaned half-run), and BEFORE the steer drain + currentInput rebuild — exactly matching SEAMS §1B and §2A, so a breaching turn never splices a steer it won't send. Abort path (handleAbort) is untouched and distinct from the budget terminal, matching the test assertion that a budget breach is `phase:'error'` and never an `aborted` action.\n\nAll new wiring lands on non-frozen surfaces (TurnRunnerDeps local interface, StreamingTurnDeps/Controls, Settings, StatusLineState/selectStatusLine which is W3-PROPOSED, slashCommands/submit, StatusLine chip). claude-cli interlock (Constitution I.2) respected: the guard/steer only act at the raw-API re-entry boundary and are inert/queued-to-next-submit on claude-cli per the documented comments; no live `claude -p` render interruption introduced. Gate evidence (tsc 0, vitest 376/376) is consistent with the additive surface. escalate=false: no frozen-seam or contract change requiring human sign-off.",
      "citation": "src/core/events.ts:46,83-84; src/core/reducer.ts:310-324; src/core/contracts.ts:17,23; src/agent/turnRunner.ts:270-340 (budget check 321-327, steer drain 335-340); git diff --stat (events.ts/reducer.ts/contracts.ts = empty)"
    },
    {
      "judge": "ui-cohesion",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "All UI-cohesion claims verified against the worktree. The tools:used/max status chip (src/ui/StatusLine.tsx:75-88) sits in the same gap={1} flex row between the mode: and cmp: chips, reuses the same wrap={textWrap} discipline, and its render guard (max!==undefined && used>0, src/ui/StatusLine.tsx:76-77) exactly mirrors the existing cmp: conditional so the runaway guard is VISIBLE, never a silent/empty chip. Theme tokens info and warning both exist (src/ui/theme.ts:34,65,67,138,140), so token('warning'/'info') at StatusLine.tsx:81-82 resolve; the 80% warn-tint flip is consistent with the existing context-bar warn pattern. selectStatusLine passes toolBudget as a pure render-only passthrough on the W3-PROPOSED StatusLineState (src/core/selectors.ts:177,195) with no frozen-seam touch; app.tsx supplies {used,max:undefined} when unbounded and the StatusLine guard suppresses the chip. The new steer slash command renders correctly through the unified palette (primary /name + secondary description, src/ui/UnifiedCommandPalette.tsx:89-91). Steer render reuses the existing user-submit action for in-transcript rendering; distinct steer styling is explicitly deferred in SEAMS section 4, an accepted design decision. No cohesion defects.",
      "citation": "src/ui/StatusLine.tsx:75-88; src/ui/theme.ts:34,65,67,138,140; src/core/selectors.ts:177,195; src/ui/UnifiedCommandPalette.tsx:89-91"
    }
  ],
  "advisories": [
    {
      "judge": "correctness",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "No HARD issues found. Two ADVISORY findings only.\n\nADVISORY 1 — src/agent/turnRunner.ts:260-322 — Mid-batch budget over-run. The inner for-loop (line 260) runs all K tool calls in one assistant response to completion before the ceiling check fires at line 322. If K pushes the cumulative count past maxToolCalls, all K tools execute before detection. Primary guard use-case (one tool per response, runaway loop prevention) is fully correct; the over-run risk is bounded by tools-per-response and is consistent with the SEAMS (\"after tools have run\"), but is undocumented.\n\nADVISORY 2 — src/hooks/useStreamingTurn.ts:393 — setToolCallsThisTurn(0) is a React state setter (async re-render schedule). Between submit() starting and next repaint, the tools:N/max chip may briefly show the prior turn's count. Cosmetic and self-correcting on next frame.\n\nAll other checks pass: frozen files (events.ts, reducer.ts, contracts.ts) untouched; budget guard placement correct (after deferredToolUseDone dispatch + tool-status events, before steer drain, before currentInput mutation); breach-check-then-break ordered before steer drain per spec; double-exposure analysis clean (live loop reads from steerQueueRef, next submit reads from committed, queue cleared before runTurn starts); parseSteerArg word-boundary regex correctly rejects /steering; maxToolCalls absent from DEFAULT_SETTINGS; parseMaxToolCalls correctly rejects 0, negatives, NaN, Infinity, floats; new test count +27 verified (it.each generates 4 subtests); all test assertions are structurally sound against TurnMessage union and ScriptedClient.inputs capture pattern.",
      "citation": "turnRunner.ts:260-322 (mid-batch budget over-run); useStreamingTurn.ts:393 (async state reset); turnRunner.ts:294-328 (guard ordering); useStreamingTurn.ts:366-376 (steer double-exposure); app.tsx:87 (word-boundary regex); config.ts:48-66 (DEFAULT_SETTINGS); config.ts:172-175 (parseMaxToolCalls)"
    },
    {
      "judge": "assumptions",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "No undeclared scope decisions or silent assumptions found. Every assumption the SEAMS flagged is either enforced in code or explicitly declared in a comment. I verified the central risk (backend asymmetry) directly: claudeCliClient.ts:733 maps tool_use->'end' and turnRunner.ts:240 breaks on non-tool_use, so on claude-cli the executor loop, onIteration (turnRunner.ts:275-276), budget breach (turnRunner.ts:322), and drainSteer are all unreachable — the budget is inert and the tools: chip stays hidden by construction, exactly as SEAMS section 0/1 claim, with no misleading-UI assumption. The /steer overlay-open double-fire is handled, not assumed away: app.tsx:453-460 intercepts before the overlay-clear branch and before turn.submit, while runSlashCommand case 'steer' (app.tsx:350-355) only closes the overlay, so injection fires exactly once (declared at app.tsx:451-452). Reset/no-double-exposure logic in useStreamingTurn (submit resets toolCallsThisTurn + clears steerQueueRef; steer commits via user-submit; live loop reads queue not committed) matches SEAMS 2A. Frozen seams events.ts/reducer.ts/contracts.ts untouched. Advisory only: config.ts:29-36 doc omits the SEAMS 'Hermes default ~90' hint (correctly not added to DEFAULT_SETTINGS, unbounded preserved); config.mode.test.ts adds 9 cases vs prose '+8' due to it.each fan-out (gate total 376 is authoritative). Neither affects scope.",
      "citation": "src/providers/claudeCliClient.ts:733 (cliStopReason maps tool_use->end); src/agent/turnRunner.ts:240 (break on non-tool_use), :275-276 (onIteration), :322 (budget breach), :330-336 (drainSteer splice); src/app.tsx:350-355 (runSlashCommand case steer), :453-460 (submit /steer intercept ordering); src/hooks/useStreamingTurn.ts:386-400 (steer commit + reset); src/services/config.ts:29-36 (doc), :172-176 (parseMaxToolCalls), :358-366 (env override); src/ui/StatusLine.tsx:75-88 (chip guard used>0 && max!==undefined)"
    },
    {
      "judge": "complexity",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "Frozen seams confirmed clean (empty diff for events.ts, reducer.ts, contracts.ts). All changes are additive with optional fields. Budget counter (turnRunner.ts:96) is per-runTurn by construction. Breach check (turnRunner.ts:322-328) fires after the full tool batch commits and before currentInput update — no orphaned tool calls. Steer two-path design (steerQueueRef for live loop, user-submit dispatch for committed persistence) prevents double-injection within one turn and prevents loss across turns. Steer intercept (app.tsx:453-460) is correctly ordered before the generic slash branch. Config parser (config.ts:174-175) mirrors parseCompactionKeepBudget exactly. 27 new tests cover all seams including the load-bearing steer-position assertion (coordinator.test.ts:566). Solution is minimal: no new event variants, no reducer changes, no DEFAULT_SETTINGS entries. Two advisory findings: (1) budget check is per-response-batch not per-individual-tool (turnRunner.ts:260-322) — operators should set limit accounting for max parallel tools; (2) palette-selected /steer silently closes overlay with no UX feedback (app.tsx:350-355). Neither warrants a block.",
      "citation": "turnRunner.ts:96,322-328; useStreamingTurn.ts:181,366-376,394; app.tsx:350-355,453-460; config.ts:174-175; StatusLine.tsx:75-88; coordinator.test.ts:566"
    }
  ]
}
