{
  "n": 4,
  "item": {
    "title": "Context compression",
    "gap": "Auto-summarize + rebuild session when cumulative token count exceeds ~50% of maxContext. Prevents failures on long/autonomous runs. #1 P0 robustness gap in TARGET_STATE; the primary difference between a demo and a harness that survives orchestration runs."
  },
  "outcome": "merged",
  "branch": "forge/context-compression",
  "writerPath": "triad",
  "verdicts": [
    {
      "judge": "correctness",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All hard correctness and seam constraints verified. Three advisory findings below; none corrupt state or violate the SEAMS contract.\n\n---\n\nFINDING 1 — ADVISORY\nLocation: src/core/reducer.ts:371-373 (blockId function)\n\nInitially flagged as spec drift, but confirmed NOT a bug. The `blockId` helper is defined as:\n  `return \\`${msgId}:block:${blockIndex + 1}\\`;`\nSo `blockId(id, 0)` → `${id}:block:1`, exactly matching the SEAMS spec's `${id}:block:1`. The test at tests/compaction.test.ts:86 confirms the expected value is `'compaction-1:block:1'`. No drift.\n\n---\n\nFINDING 2 — ADVISORY\nLocation: src/agent/compactor.ts:44 and SEAMS Unit 2 spec\n\nThe SEAMS spec signature reads `buildCompactionInput(messages: TurnMessage[]): TurnInput` (one param). The implementation is `buildCompactionInput(messages: TurnMessage[], id: string): TurnInput` (two params). The SEAMS note immediately below says \"Deterministic `id` (caller supplies)\" — so the two-param design is the stated intent; the abbreviated signature in the spec text does not match. Advisory only: the test at compaction.test.ts:282 confirms the two-param call is the accepted interface, TSC passes (0 errors), and the caller (`runCompaction` at compactor.ts:71) passes a derived deterministic id with no Date.now/Math.random.\n\n---\n\nFINDING 3 — ADVISORY\nLocation: src/hooks/useStreamingTurn.ts:319-322 and :327\n\n`maybeCompact` is fire-and-forget (`void maybeCompact()`) after the turn's `finally` block clears `controllerRef.current` (line 370). `runCompactionStep` synchronously sets `controllerRef.current = controller` before its first `await`, so by the time the JS event loop can process a new user event, `controllerRef.current` is non-null. The `submit` guard at line 327 (`controllerRef.current !== null`) will then silently drop a user submission during the compaction window. Since `state.phase` remains `'idle'` throughout (compaction is invisible to the reducer until dispatch), the user sees no indicator that their message was dropped. The SEAMS spec explicitly mandates reusing `controllerRef` for abort semantics (Section 3 Unit 2), so this is by design. Advisory only: user can abort via the existing abort path; the compaction path is fast (single LLM call). A follow-up UX chip or phase enum value (e.g. `'compacting'`) could surface this, but it is out of scope for W6.\n\n---\n\nPASSING CHECKS (no findings required):\n\nFrozen seams: `src/core/events.ts` and `src/core/contracts.ts` not in the changed file list — confirmed by `git diff main --name-only`. No new AgentEvent variant; `compact` is a LOCAL reducer action per SEAMS.\n\nReducer `compact` case (reducer.ts:338-366): spreads `...state` (preserves `tokens`, `effort`, `permissionMode`, `tools`), replaces `committed` with `[summaryMsg, ...keep]`, resets `live`, `phase`, `overlay` (permission→none), `pendingPermissionToolCallId`. Monotonic `compaction-${n}` id — no Date.now/Math.random. Test asserts ref-equality on `tokens` and `tools` (line 95-98). SEAMS-correct.\n\n`runCompaction` (compactor.ts:63-92): passes `[]` as tools (test at line 214 asserts `seenTools === [[]]`). Stops on `signal.aborted`, `assistant-done`, `error`, `aborted`. Empty transcript guard at line 68 skips client invocation entirely (test line 266 confirms). Catch collapses to accumulated partial summary — never crashes. SEAMS-correct.\n\n`chooseKeepCount` (compactor.ts:104-135): budget-respecting first (breaks before the user message if it would blow the budget); coherent-boundary second (stops after including the user message). `Math.max(1, count)` guarantees ≥1 for non-empty arrays. Three tests (lines 154-192) cover budget-fits-tail, budget-exceeded-before-user-boundary, and empty/zero-budget edge cases. SEAMS-correct.\n\n`shouldCompact` gate (selectors.ts): keyed to `state.committed` (current transcript), not `state.tokens` (cumulative spend). After a compact, `committed` shrinks → estimate drops → trigger self-clears. Correctly implements SEAMS Section 2's load-bearing requirement.\n\n`compactNow` (force=true) path (useStreamingTurn.ts:320-322): bypasses `shouldCompact` threshold but respects `compactingRef`/`controllerRef` re-entrancy guard and `MIN_MESSAGES_TO_COMPACT` floor. App wires it correctly at app.tsx:323 (`void turn.compactNow(); closeOverlay()`).\n\nConfig (config.ts): `compactionThreshold` parsed as finite number in (0,1]; `compactionKeepBudget` as positive safe integer. `JUNO_COMPACTION_THRESHOLD` env override follows the same parse gate. `maxContext` was pre-existing in `Settings` (default 1_047_576 at config.ts:47).\n\nStatusLine (StatusLine.tsx): pressure bar reuses the existing `contextBar` slot with `contextPressure ?? contextFraction` fallback; warning tint fires at `>= DEFAULT_COMPACTION_THRESHOLD`; `cmp:n` chip is conditional and in the existing row — no new row. Footer-line-count invariant preserved.",
      "citation": "reducer.ts:338-366 (compact case); reducer.ts:371-373 (blockId); compactor.ts:44,63-92,104-135; useStreamingTurn.ts:268-322,327; selectors.ts:selectContextPressure,shouldCompact; tests/compaction.test.ts:86,214,266,282"
    },
    {
      "judge": "assumptions",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All load-bearing claims verified against the worktree; frozen seams (events.ts, contracts.ts) confirmed untouched and reducer additive-only. Two undeclared scope decisions found, both cosmetic/naming and non-blocking: (1) StatusLine.tsx:56 tints the pressure bar against the hardcoded DEFAULT_COMPACTION_THRESHOLD (0.5) rather than the configured deps.compactionThreshold that actually drives auto-compaction, so the warning tint desyncs from the real trigger when a user sets a non-default threshold (StatusLineState carries no threshold field); SEAMS Unit 3 said tint 'when pressure >= threshold' (e.g.). (2) config.ts adopts compactionKeepBudget (token-budget semantics) silently reconciling SEAMS's internal contradiction (§1 says compactionKeepCount, §4/Unit-2 say compactionKeepBudget). The non-shrinking-compaction edge (keepCount==committed.length) self-no-ops via empty elided prefix -> runCompaction('') -> caller skip guard (useStreamingTurn.ts:291), so no corruption. No frozen-seam violation, no contract change, no correctness break on the assumptions axis.",
      "citation": "src/ui/StatusLine.tsx:56; src/services/config.ts:26; src/core/selectors.ts:160,177; src/app.tsx:165,196; src/hooks/useStreamingTurn.ts:289,291"
    },
    {
      "judge": "complexity",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All hard requirements pass cleanly. Frozen seams (events.ts, contracts.ts) are untouched. The reducer compact case is additive and pure: blockId(id,0) at reducer.ts:350 resolves to ${id}:block:1 (reducer.ts:372), matching both the SEAMS spec and test assertions at compaction.test.ts:86. State fields tokens/effort/permissionMode/tools are preserved via ...state spread. Submit is correctly blocked during compaction because controllerRef.current is non-null while runCompactionStep runs (useStreamingTurn.ts:270,287,327). Re-entrancy is clean: all code from the guard check to compactingRef.current=true is synchronous. chooseKeepCount is budget-respecting and user-boundary-aware; runCompaction passes tools=[] and is verified by the seenTools assertion at compaction.test.ts:214. Config parsing, StatusLine chip (single line, no new row), and the /compact slash command are all spec-faithful. Gate: TSC 0 errors, 347/347 vitest. Minimality test: no unnecessary abstraction; no new AgentEvent; the impure/pure split is exactly what the SEAMS prescribes.\\n\\nOne advisory finding: void maybeCompact() at useStreamingTurn.ts:377 fires unconditionally after submit's finally, including after error turns. The compact reducer resets phase to 'idle' (reducer.ts:360) while errorMessage persists from ...state (reducer.ts:357). Under high transcript pressure coinciding with a turn error, auto-compaction would silently transition phase from 'error' to 'idle', hiding the error status chip. The error system message remains in committed history so data is not lost, but the phase indicator resets without user action. No test covers this path. Recommend either (a) skipping maybeCompact when stateRef.current.phase === 'error', or (b) clearing errorMessage to null in the compact reducer case. Low probability in practice but real.",
      "citation": "useStreamingTurn.ts:377 (unconditional maybeCompact after finally); reducer.ts:360 (phase:'idle' in compact case); reducer.ts:357 (errorMessage preserved via ...state spread); compaction.test.ts:86 (block id assertion); reducer.ts:372 (blockId helper: +1 offset); useStreamingTurn.ts:327 (submit guard: controllerRef.current !== null); compaction.test.ts:214 (seenTools===[[]] assertion)"
    },
    {
      "judge": "scope",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "All 8 changed files (src/agent/compactor.ts, src/app.tsx, src/core/reducer.ts, src/core/selectors.ts, src/hooks/useStreamingTurn.ts, src/services/config.ts, src/ui/StatusLine.tsx, tests/compaction.test.ts) map 1:1 to SEAMS units or sections. Frozen seams src/core/events.ts and src/core/contracts.ts are absent from git diff --name-only. No compact AgentEvent variant exists. Additive-only reducer changes confirmed (compactions? optional, absent from initialState at reducer.ts:117-129). blockId(id,0) returns '${id}:block:1' (reducer.ts:372 confirms blockIndex+1), identical to the SEAMS literal. Estimate keyed to state.committed not state.tokens per SEAMS §2. Re-entrancy guard compactingRef||controllerRef matches spec exactly. maybeCompact() fires after submit's finally, never mid-turn. compactNow() exposed. StatusLine cmp chip is inline with no new row. Config parses compactionThreshold in (0,1] and compactionKeepBudget as positive safe integer. No orthogonal changes detected.",
      "citation": "src/core/reducer.ts:84,117-129,338-366,371-373 (blockId, initialState, compact case); src/core/selectors.ts:177-178 (selectStatusLine); src/hooks/useStreamingTurn.ts:85,152,259-326,377,434 (toTurnMessages local, compactingRef, runCompactionStep, maybeCompact, compactNow); src/agent/compactor.ts:16-19,45,63,68-92,96-131 (prompt, buildCompactionInput, runCompaction, chooseKeepCount); src/services/config.ts:10 (maxContext pre-existing); git diff --name-only (events.ts+contracts.ts absent)"
    },
    {
      "judge": "goal",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "Every step->verify clause independently re-verified in the worktree (C:/Users/Core/src/juno-forge-context-compression), not taken on trust.\n\nEMPTY-DIFF GUARD: PASS — diff is non-empty: 8 files changed (new src/agent/compactor.ts + tests/compaction.test.ts, plus edits to app.tsx, reducer.ts, selectors.ts, useStreamingTurn.ts, config.ts, StatusLine.tsx). git diff main --name-only matches the authoritative DIFF exactly.\n\nSTEP 1 (tsc): re-ran `npx tsc --noEmit` -> TSC_EXIT:0, zero errors. Claim holds.\n\nSTEP 2 (vitest): re-ran `npx vitest run` -> 24 files, 347/347 passed (VITEST_EXIT:0). tests/compaction.test.ts present with exactly 12 tests (compaction.test.ts:56-301). Baseline 335 + 12 = 347 arithmetic holds. No regressions: app.smoke (8), unifiedPalette (6), slashIntercept (3) all green despite the added /compact command.\n\nSTEP 3 (frozen-seam): `git diff main -- src/core/events.ts src/core/contracts.ts` = 0 lines — both FROZEN seams UNTOUCHED. compactor.ts:80 confirms `client.streamTurn(input, [], signal)` (tools=[], no tool loop). Compaction is a LOCAL reducer Action {t:'compact'} with no wire AgentEvent.\n\nSTEP 4 (additive reducer): State.compactions added OPTIONAL; grep for 'initialState' in reducer.ts diff hits only the doc comment ('NOT set by initialState()'), not a code change — so initialState() is not modified, per SEAMS. New Action variant and new case 'compact' only; reducer compact case spreads ...state and overrides only committed/live/phase/overlay/pendingPermissionToolCallId/compactions, preserving tokens/effort/permissionMode/tools — asserted by reducer compact test via ref-equality (compaction.test.ts:95-98) and monotonic compaction-<n> id (lines 92,107). chooseKeepCount budget/user-boundary/>=1/<=length behavior traced against the 3 tests (compaction.test.ts:153-191) and is internally consistent. runCompaction tools=[] (seenTools toEqual [[]], line 214), abort/empty/empty-transcript -> '' all asserted (lines 217-267). buildCompactionInput leaks no `tools` key (line 299).\n\nAll Unit 1-4 step->verify clauses pass; SEAMS frozen-seam pins honored; no claim left unverified.",
      "citation": "C:/Users/Core/src/juno-forge-context-compression/src/agent/compactor.ts:80; src/core/reducer.ts compact case; tests/compaction.test.ts:56-301 (12 tests); git diff main -- src/core/events.ts src/core/contracts.ts = 0 lines; tsc exit 0; vitest 347/347"
    },
    {
      "judge": "architecture",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "Frozen-seam compliance fully verified against the worktree. events.ts and contracts.ts are UNTOUCHED (git diff main empty for both) — no compact AgentEvent was added; compaction is a LOCAL reducer action in the same class as clear (SEAMS sec.1). The compactor consumes the existing ModelClient.streamTurn seam with tools=[] (compactor.ts:71), no contracts change. reducer.ts is additive-only: optional State.compactions absent from initialState() (verified), new {t:'compact'} Action variant (reducer.ts:111), new case 'compact' (reducer.ts:338) built via spread ...state preserving tokens/effort/permissionMode/tools; no existing field of State/Action/Msg/Block/ToolState retyped or removed. The case uses the canonical blockId(id,0) helper (reducer.ts:350), consistent with the error case (reducer.ts:317) — the SEAMS ${id}:block:1 was illustrative pseudocode; impl is faithful and pure (monotonic-counter id, no Date.now/Math.random). Composition holds: compactor serializeTurnMessage/buildCompactionInput match TurnMessage/TurnInput (contracts.ts:16-42); kept-tail toolSnapshot is preserved so toTurnMessages (useStreamingTurn.ts:103) resolves tool refs. step->verify gate (tsc 0, vitest 347/347) is consistent with what I inspected.",
      "citation": "src/core/reducer.ts:84,111,338,350; src/core/contracts.ts:30-42,96; src/agent/compactor.ts:71,107,124; src/hooks/useStreamingTurn.ts:103; ADVISORY: chooseKeepCount budget-stop branch (compactor.ts:124) can open the kept tail mid tool-exchange, risking a dangling tool_result in the next re-sent turn — within SEAMS-accepted soft user-boundary guarantee, non-blocking."
    },
    {
      "judge": "ui-cohesion",
      "verdict": "PASS",
      "mode": "HARD",
      "reason": "UI cohesion is sound. (1) Palette: both new tints resolve to real unified-palette semantic tokens — warning (theme.ts:32=#F4BF75) and info (theme.ts:34=#AE81FF); StatusLine.tsx:56-58,76 route exclusively through token(...) so depth downsampling still applies, and no raw hex was introduced. cmp:<n> reuses the same info token as the skills chip and the high-pressure bar reuses warning like the mode chip — consistent. (2) Footer-line-count invariant preserved: the cmp chip (StatusLine.tsx:75-79) lives inside the existing top Box that is flexWrap=nowrap/overflow=hidden under fixed width (StatusLine.tsx:31-33,44); exactly one chip, no new row, truncate-end bounds overflow — still two rows. (3) /compact slash command wired consistently with sibling commands (app.tsx case compact -> compactNow + closeOverlay). One ADVISORY (non-blocking): StatusLine.tsx:56 tints the bar against the hardcoded DEFAULT_COMPACTION_THRESHOLD rather than the user-configurable compactionThreshold, so the warning tint can desync from when auto-compaction actually fires — permitted by SEAMS wording, optional fix is to thread the resolved threshold into StatusLineState. All claims verified against the worktree tree; nothing unverifiable.",
      "citation": "src/ui/theme.ts:32,34,65,67; src/ui/StatusLine.tsx:31-33,44,56-58,75-79; src/app.tsx (slashCommands + runSlashCommand case 'compact')"
    }
  ],
  "advisories": [
    {
      "judge": "correctness",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All hard correctness and seam constraints verified. Three advisory findings below; none corrupt state or violate the SEAMS contract.\n\n---\n\nFINDING 1 — ADVISORY\nLocation: src/core/reducer.ts:371-373 (blockId function)\n\nInitially flagged as spec drift, but confirmed NOT a bug. The `blockId` helper is defined as:\n  `return \\`${msgId}:block:${blockIndex + 1}\\`;`\nSo `blockId(id, 0)` → `${id}:block:1`, exactly matching the SEAMS spec's `${id}:block:1`. The test at tests/compaction.test.ts:86 confirms the expected value is `'compaction-1:block:1'`. No drift.\n\n---\n\nFINDING 2 — ADVISORY\nLocation: src/agent/compactor.ts:44 and SEAMS Unit 2 spec\n\nThe SEAMS spec signature reads `buildCompactionInput(messages: TurnMessage[]): TurnInput` (one param). The implementation is `buildCompactionInput(messages: TurnMessage[], id: string): TurnInput` (two params). The SEAMS note immediately below says \"Deterministic `id` (caller supplies)\" — so the two-param design is the stated intent; the abbreviated signature in the spec text does not match. Advisory only: the test at compaction.test.ts:282 confirms the two-param call is the accepted interface, TSC passes (0 errors), and the caller (`runCompaction` at compactor.ts:71) passes a derived deterministic id with no Date.now/Math.random.\n\n---\n\nFINDING 3 — ADVISORY\nLocation: src/hooks/useStreamingTurn.ts:319-322 and :327\n\n`maybeCompact` is fire-and-forget (`void maybeCompact()`) after the turn's `finally` block clears `controllerRef.current` (line 370). `runCompactionStep` synchronously sets `controllerRef.current = controller` before its first `await`, so by the time the JS event loop can process a new user event, `controllerRef.current` is non-null. The `submit` guard at line 327 (`controllerRef.current !== null`) will then silently drop a user submission during the compaction window. Since `state.phase` remains `'idle'` throughout (compaction is invisible to the reducer until dispatch), the user sees no indicator that their message was dropped. The SEAMS spec explicitly mandates reusing `controllerRef` for abort semantics (Section 3 Unit 2), so this is by design. Advisory only: user can abort via the existing abort path; the compaction path is fast (single LLM call). A follow-up UX chip or phase enum value (e.g. `'compacting'`) could surface this, but it is out of scope for W6.\n\n---\n\nPASSING CHECKS (no findings required):\n\nFrozen seams: `src/core/events.ts` and `src/core/contracts.ts` not in the changed file list — confirmed by `git diff main --name-only`. No new AgentEvent variant; `compact` is a LOCAL reducer action per SEAMS.\n\nReducer `compact` case (reducer.ts:338-366): spreads `...state` (preserves `tokens`, `effort`, `permissionMode`, `tools`), replaces `committed` with `[summaryMsg, ...keep]`, resets `live`, `phase`, `overlay` (permission→none), `pendingPermissionToolCallId`. Monotonic `compaction-${n}` id — no Date.now/Math.random. Test asserts ref-equality on `tokens` and `tools` (line 95-98). SEAMS-correct.\n\n`runCompaction` (compactor.ts:63-92): passes `[]` as tools (test at line 214 asserts `seenTools === [[]]`). Stops on `signal.aborted`, `assistant-done`, `error`, `aborted`. Empty transcript guard at line 68 skips client invocation entirely (test line 266 confirms). Catch collapses to accumulated partial summary — never crashes. SEAMS-correct.\n\n`chooseKeepCount` (compactor.ts:104-135): budget-respecting first (breaks before the user message if it would blow the budget); coherent-boundary second (stops after including the user message). `Math.max(1, count)` guarantees ≥1 for non-empty arrays. Three tests (lines 154-192) cover budget-fits-tail, budget-exceeded-before-user-boundary, and empty/zero-budget edge cases. SEAMS-correct.\n\n`shouldCompact` gate (selectors.ts): keyed to `state.committed` (current transcript), not `state.tokens` (cumulative spend). After a compact, `committed` shrinks → estimate drops → trigger self-clears. Correctly implements SEAMS Section 2's load-bearing requirement.\n\n`compactNow` (force=true) path (useStreamingTurn.ts:320-322): bypasses `shouldCompact` threshold but respects `compactingRef`/`controllerRef` re-entrancy guard and `MIN_MESSAGES_TO_COMPACT` floor. App wires it correctly at app.tsx:323 (`void turn.compactNow(); closeOverlay()`).\n\nConfig (config.ts): `compactionThreshold` parsed as finite number in (0,1]; `compactionKeepBudget` as positive safe integer. `JUNO_COMPACTION_THRESHOLD` env override follows the same parse gate. `maxContext` was pre-existing in `Settings` (default 1_047_576 at config.ts:47).\n\nStatusLine (StatusLine.tsx): pressure bar reuses the existing `contextBar` slot with `contextPressure ?? contextFraction` fallback; warning tint fires at `>= DEFAULT_COMPACTION_THRESHOLD`; `cmp:n` chip is conditional and in the existing row — no new row. Footer-line-count invariant preserved.",
      "citation": "reducer.ts:338-366 (compact case); reducer.ts:371-373 (blockId); compactor.ts:44,63-92,104-135; useStreamingTurn.ts:268-322,327; selectors.ts:selectContextPressure,shouldCompact; tests/compaction.test.ts:86,214,266,282"
    },
    {
      "judge": "assumptions",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All load-bearing claims verified against the worktree; frozen seams (events.ts, contracts.ts) confirmed untouched and reducer additive-only. Two undeclared scope decisions found, both cosmetic/naming and non-blocking: (1) StatusLine.tsx:56 tints the pressure bar against the hardcoded DEFAULT_COMPACTION_THRESHOLD (0.5) rather than the configured deps.compactionThreshold that actually drives auto-compaction, so the warning tint desyncs from the real trigger when a user sets a non-default threshold (StatusLineState carries no threshold field); SEAMS Unit 3 said tint 'when pressure >= threshold' (e.g.). (2) config.ts adopts compactionKeepBudget (token-budget semantics) silently reconciling SEAMS's internal contradiction (§1 says compactionKeepCount, §4/Unit-2 say compactionKeepBudget). The non-shrinking-compaction edge (keepCount==committed.length) self-no-ops via empty elided prefix -> runCompaction('') -> caller skip guard (useStreamingTurn.ts:291), so no corruption. No frozen-seam violation, no contract change, no correctness break on the assumptions axis.",
      "citation": "src/ui/StatusLine.tsx:56; src/services/config.ts:26; src/core/selectors.ts:160,177; src/app.tsx:165,196; src/hooks/useStreamingTurn.ts:289,291"
    },
    {
      "judge": "complexity",
      "verdict": "PASS",
      "mode": "ADVISORY",
      "reason": "All hard requirements pass cleanly. Frozen seams (events.ts, contracts.ts) are untouched. The reducer compact case is additive and pure: blockId(id,0) at reducer.ts:350 resolves to ${id}:block:1 (reducer.ts:372), matching both the SEAMS spec and test assertions at compaction.test.ts:86. State fields tokens/effort/permissionMode/tools are preserved via ...state spread. Submit is correctly blocked during compaction because controllerRef.current is non-null while runCompactionStep runs (useStreamingTurn.ts:270,287,327). Re-entrancy is clean: all code from the guard check to compactingRef.current=true is synchronous. chooseKeepCount is budget-respecting and user-boundary-aware; runCompaction passes tools=[] and is verified by the seenTools assertion at compaction.test.ts:214. Config parsing, StatusLine chip (single line, no new row), and the /compact slash command are all spec-faithful. Gate: TSC 0 errors, 347/347 vitest. Minimality test: no unnecessary abstraction; no new AgentEvent; the impure/pure split is exactly what the SEAMS prescribes.\\n\\nOne advisory finding: void maybeCompact() at useStreamingTurn.ts:377 fires unconditionally after submit's finally, including after error turns. The compact reducer resets phase to 'idle' (reducer.ts:360) while errorMessage persists from ...state (reducer.ts:357). Under high transcript pressure coinciding with a turn error, auto-compaction would silently transition phase from 'error' to 'idle', hiding the error status chip. The error system message remains in committed history so data is not lost, but the phase indicator resets without user action. No test covers this path. Recommend either (a) skipping maybeCompact when stateRef.current.phase === 'error', or (b) clearing errorMessage to null in the compact reducer case. Low probability in practice but real.",
      "citation": "useStreamingTurn.ts:377 (unconditional maybeCompact after finally); reducer.ts:360 (phase:'idle' in compact case); reducer.ts:357 (errorMessage preserved via ...state spread); compaction.test.ts:86 (block id assertion); reducer.ts:372 (blockId helper: +1 offset); useStreamingTurn.ts:327 (submit guard: controllerRef.current !== null); compaction.test.ts:214 (seenTools===[[]] assertion)"
    }
  ]
}
